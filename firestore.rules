rules_version = '2';

// Everything is accessed server-side through the Admin SDK (which bypasses these
// rules entirely), so clients get no direct access to Firestore at all.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
